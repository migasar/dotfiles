# Get External IP / Internet Speed
alias myip="curl https://ipinfo.io/json" # or /ip for plain-text ip
alias speedtest="curl -s https://raw.githubusercontent.com/sivel/speedtest-cli/master/speedtest.py | python -"

# Quickly serve the current directory as HTTP
alias serve='ruby -run -e httpd . -p 8000' # Or python -m SimpleHTTPServer :)

# Lock the commands to ensure that they use their Rosetta variant, instead of ARM
alias brew='arch -x86_64 brew'
alias make='arch -x86_64 make'
alias pip='arch -x86_64 pip'
alias pyenv='arch -x86_64 pyenv'
alias zsh='arch -x86_64 zsh'

# Get Gcloud private key of the active project
export GOOGLE_APPLICATION_CREDENTIALS=/Users/mica/code/migasar/gcp/lewagon-challenges-279a994e2c37.json
export GOOGLE_APPLICATION_CREDENTIALS=/Users/mica/code/migasar/gcp/wagon-data-610-c64059b2a1b5.json
